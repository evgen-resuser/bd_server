package org.evgen.bd_server.repository;

import org.evgen.bd_server.model.Organize;
import org.springframework.data.jpa.repository.JpaRepository;

public interface OrganizeRepository extends JpaRepository<Organize, Integer> {
}
